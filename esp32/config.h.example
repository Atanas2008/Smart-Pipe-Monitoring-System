/*
 * smart_pipe_monitor_config.h
 *
 * Copy this file to config.h and fill in your credentials.
 * config.h is listed in .gitignore and should never be committed.
 */

#ifndef CONFIG_H
#define CONFIG_H

// WiFi credentials
#define WIFI_SSID     "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

// Backend server URL
#define SERVER_URL    "http://YOUR_SERVER_IP:3000/api/sensors"

#endif // CONFIG_H
